[pytest]
# Pytest configuration for Thai Regulatory AI

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Directories to search for tests
testpaths = tests

# Minimum Python version
minversion = 3.8

# Console output options
# -m "not integration": integration-marked tests fire real, paid LLM calls
# (e.g. test_rag_faithfulness.py) — excluded from the default run so a plain
# `pytest` never costs money or needs a real OPENROUTER_API_KEY. Run them
# deliberately with `pytest -m integration`.
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
    -m "not integration"

# Coverage options (if using pytest-cov)
# addopts = 
#     -v
#     --cov=code
#     --cov-report=html
#     --cov-report=term-missing
#     --cov-fail-under=70

# Markers for organizing tests
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (slower, require services)
    slow: Slow tests
    api: API endpoint tests
    cache: Cache-related tests
    security: Security tests
    performance: Performance/load tests

# Timeout for tests (in seconds)
timeout = 300

# Ignore certain files/directories
norecursedirs = .git .tox dist build *.egg venv local_chroma_v2 chroma_db __pycache__
