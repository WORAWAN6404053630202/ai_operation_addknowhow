# Bot Flow — คู่มือทีม Operations

> น้องสุดยอด — ผู้ช่วยที่ปรึกษาธุรกิจร้านอาหาร
> อัพเดท: 2026-04-21

---

## น้องสุดยอดคืออะไร?

น้องสุดยอดคือระบบ AI ที่ช่วยเจ้าของร้านอาหารเรื่องกฎหมายและการดำเนินธุรกิจ เช่น:
- ขอใบอนุญาตต่างๆ (ใบอนุญาตจัดตั้งสถานที่จำหน่ายอาหาร, ใบทะเบียนพาณิชย์ ฯลฯ)
- ภาษี VAT / ภาษีป้าย
- ประกันสังคม / ขึ้นทะเบียนนายจ้าง
- การสมัครระบบชำระเงิน QR Payment
- แนะนำการเปิดร้าน, คาเฟ่, เบเกอรี่

**หลักการสำคัญ:** บอทตอบจากข้อมูลที่เรามีในฐานข้อมูลเท่านั้น — ไม่คาดเดา ไม่แต่งเติม

---

## ภาพรวม Flow การสนทนา

```
User เริ่มแชท
    │
    ▼
[1] บอททักทาย + แสดงเมนู 5 หัวข้อ (สุ่มจากฐานข้อมูล)
    │
    ▼
[2] User เลือกหัวข้อ หรือพิมพ์คำถาม
    │
    ▼
[3] บอทวิเคราะห์ว่าเป็นอะไร
    ├─── ทักทาย / พูดทั่วไป → ตอบสั้นๆ ไม่ค้นข้อมูล
    ├─── ถามเรื่องกฎหมาย/ขั้นตอน → ค้นข้อมูล แล้วตอบ/ถามเพิ่ม
    └─── ขอแบบละเอียด → เปลี่ยนเป็นโหมดวิชาการ (อัตโนมัติ)
    │
    ▼
[4] ถ้าต้องการข้อมูลเพิ่ม → บอทจะถามทีละข้อ (max 2 ข้อ)
    │
    ▼
[5] บอทตอบพร้อมขั้นตอน เอกสาร ค่าธรรมเนียม และลิงก์
```

---

## โหมดการทำงาน 2 โหมด

### โหมด Practical (โหมดปกติ)
- **ลักษณะ:** ตอบสั้น ตรงประเด็น กระชับ
- **เหมาะกับ:** ผู้ใช้ที่ต้องการคำตอบเร็ว รู้ว่าต้องการอะไร
- **ตัวอย่างคำตอบ:** ขั้นตอน + เอกสาร + ค่าธรรมเนียม + ลิงก์ ในข้อความเดียว

### โหมด Academic (โหมดละเอียด)
- **ลักษณะ:** อธิบายละเอียด เป็นขั้นเป็นตอน มีเมนูให้เลือกดูแต่ละส่วน
- **เหมาะกับ:** ผู้ใช้ที่ต้องการเข้าใจลึก ต้องการข้อมูลครบถ้วนทุกด้าน
- **วิธีเข้า:** พิมพ์ "ขอแบบละเอียด", "อธิบายเพิ่มเติม", "แบบวิชาการ"
- **วิธีออก:** พิมพ์ "โอเค", "เข้าใจแล้ว", "ขอบคุณ" → กลับโหมดปกติอัตโนมัติ

---

## Flow ละเอียด — ทุกกรณี

### กรณีที่ 1: User ทักทาย

```
User: "สวัสดีครับ" / "หวัดดี" / "ขอบคุณ" / (ข้อความว่าง)
    │
    ▼
บอทตอบสั้นๆ เป็นกันเอง
"สวัสดีครับ! มีอะไรให้ช่วยไหมครับ 😊"
    │
    ▼
ไม่มีการค้นหาข้อมูลใดๆ ทั้งสิ้น
```

---

### กรณีที่ 2: User เลือกหัวข้อจากเมนู

```
บอทแสดงเมนู:
  1) ขอใบอนุญาตจัดตั้งสถานที่จำหน่ายอาหาร
  2) วิธีการลงทะเบียน QR-Payment API
  ...
    │
User พิมพ์: "2" หรือ "QR Payment"
    │
    ▼
บอทค้นหาข้อมูล QR Payment
    │
    ▼
บอทถามข้อมูลที่จำเป็น (ทีละข้อ):
  ├─ "ธุรกิจของคุณเป็นรูปแบบใดครับ?"
  │   1) นิติบุคคล
  │   2) บุคคลธรรมดา
  │
  └─ User ตอบ "1" (นิติบุคคล)
       │
       ▼
  บอทค้นหาข้อมูลเพิ่มเติม (filter เฉพาะนิติบุคคล)
       │
       ▼
  บอทตอบพร้อมขั้นตอนและเอกสาร
```

**หมายเหตุ:** ถ้า User บอกชื่อธนาคารหรือประเภทธุรกิจมาในคำถามแล้ว บอทจะไม่ถามซ้ำ
เช่น "สมัคร QR Payment กับธนาคารไทยพาณิชย์" → บอทรู้ว่าเป็นธนาคารไทยพาณิชย์เลย

---

### กรณีที่ 3: User ถามคำถามโดยตรง (ไม่เลือกเมนู)

```
User: "จะขอใบอนุญาตจำหน่ายสุราต้องทำยังไงบ้าง"
    │
    ▼
บอทค้นหาข้อมูล
    │
    ▼
ถามข้อมูลที่จำเป็น (ถ้าคำตอบต่างกันตามบริบท):
  "ธุรกิจของคุณเป็นรูปแบบใดครับ?"
  1) นิติบุคคล
  2) บุคคลธรรมดา
    │
    ▼
User ตอบ → บอทตอบเต็ม
```

---

### กรณีที่ 4: User ถามหลายเรื่องพร้อมกัน (Broad Question)

```
User: "อยากเปิดร้านเบเกอรี่ ต้องทำอะไรบ้าง"
User: "ร้านอาหารต้องเสียภาษีอะไรบ้าง"
    │
    ▼
บอทค้นข้อมูลจากหลายมุม (2 รอบ):
  รอบ 1: ค้นตามคำถาม
  รอบ 2: ค้นเรื่องกฎหมาย/ใบอนุญาต (เพื่อให้ครบ)
    │
    ▼
บอทตอบแบบภาพรวม แบ่งเป็นหัวข้อย่อย:
  📋 ใบอนุญาตที่ต้องขอ
  💼 การจดทะเบียน
  💰 ภาษีที่ต้องรู้
  ...
    │
    ▼
บอทถามว่า "อยากรู้ส่วนไหนเพิ่มเติม?"
```

**ข้อสังเกต:** บอทไม่ถามว่า "เป็นนิติบุคคลหรือบุคคลธรรมดา?" ในขั้นตอนนี้
ถามเฉพาะเมื่อ User เลือกดูรายละเอียดของแต่ละใบอนุญาตแล้ว

---

### กรณีที่ 5: User ถามต่อเนื่อง (Follow-up)

```
[สนทนาก่อนหน้า: ถามเรื่องจดทะเบียนพาณิชย์ บริษัทจำกัด]
    │
User: "แล้วเอกสารที่ต้องใช้มีอะไรบ้าง"
    │
    ▼
บอทใช้ข้อมูลเดิมที่ค้นไว้แล้ว
ไม่ถามรูปแบบธุรกิจซ้ำ (รู้อยู่แล้วว่าบริษัทจำกัด)
    │
    ▼
บอทตอบเฉพาะรายการเอกสาร
```

---

### กรณีที่ 6: User ขอโหมดละเอียด (Academic)

```
User: "ขอแบบละเอียดกว่านี้" / "อธิบายเชิงลึก" / "ตามกฎหมายเป๊ะๆ"
    │
    ▼
บอทเปลี่ยนเป็นโหมดละเอียดอัตโนมัติ (ไม่ถามยืนยัน)
    │
    ▼
Phase 1: ถามข้อมูลที่จำเป็น (ครั้งเดียว ทุกข้อในข้อความเดียว)
  เช่น "รูปแบบธุรกิจ? ที่ตั้งร้าน? ประเภทการดำเนินการ?"
    │
User ตอบ
    │
    ▼
Phase 2: บอทแสดงเมนูส่วนที่มีข้อมูล:
  1) ขั้นตอนการดำเนินการ
  2) เอกสารที่ต้องใช้
  3) ค่าธรรมเนียม
  4) ระยะเวลา
  5) ช่องทางยื่น / ลิงก์
    │
User เลือกส่วนที่ต้องการ
    │
    ▼
Phase 3: บอทตอบละเอียดในส่วนนั้น
    │
    ▼
Phase 4: ถามว่าต้องการดูส่วนอื่นอีกมั้ย
  ├─ ถ้าถามเพิ่ม → ตอบต่อ (ยังอยู่โหมดละเอียด)
  └─ ถ้า "โอเค/ขอบคุณ" → กลับโหมดปกติอัตโนมัติ
```

---

### กรณีที่ 7: User พิมพ์ผิด / พิมพ์ไม่ชัด

```
User: "ๅๆๆ" / "asdf" / ข้อความที่อ่านไม่รู้เรื่อง
    │
    ▼
บอทแจ้ง: "ดูเหมือนจะพิมพ์ผิดนะครับ ลองพิมพ์ใหม่ได้เลยครับ"
    │
    ▼
pending_slot ยังคงอยู่ (ไม่หายไป)
User พิมพ์ใหม่ → flow ต่อตามปกติ
```

---

### กรณีที่ 8: Session ครบ 7 รอบ

```
User ส่งข้อความรอบที่ 8
    │
    ▼
บอท: "ขออภัยครับ เราได้ตอบคำถามครบ 7 รอบแล้ว
       กรุณาเปิดเซสชันใหม่เพื่อถามคำถามเพิ่มเติมครับ"
    │
    ▼
Client ต้องเรียก POST /reset หรือ POST /greeting เพื่อเริ่มใหม่
```

---

### กรณีที่ 9: ถามซ้ำ (Cache Hit)

```
User ถามคำถามเดิมที่เคยถามแล้วในเซสชันนี้
    │
    ▼
ระบบดึงคำตอบจาก cache (ไม่เรียก AI ซ้ำ)
    │
    ▼
ตอบเร็วกว่า + ประหยัดค่าใช้จ่าย
    │
    ▼
หมายเหตุ: ถ้ามี pending_slot อยู่ หรือมีข้อมูลรูปแบบธุรกิจแล้ว
         จะ skip cache เพื่อให้ตอบถูกต้องตามบริบท
```

---

### กรณีที่ 10: Rate Limit / ใช้มากเกินไป

```
User ส่งข้อความถี่เกินไป
    │
    ▼
HTTP 429 Too Many Requests
"Too many requests. Please wait X seconds."
    │
    ▼
Header มี Retry-After บอกว่าต้องรอกี่วินาที
```

---

## สิ่งที่บอททำไม่ได้ (Guardrails)

| บอทจะไม่ทำ | เหตุผล |
|---|---|
| ตอบเรื่องที่ไม่มีในฐานข้อมูล | Evidence-first policy |
| คิดค้น URL หรือลิงก์เอง | ใช้เฉพาะลิงก์จากข้อมูลจริง |
| ถามข้อมูลเดิมซ้ำ | จำ collected_slots ตลอด session |
| เปลี่ยนโหมดเองโดยพลการ | Supervisor ควบคุม |
| แก้ไขประวัติการสนทนาย้อนหลัง | Append-only state |
| ตอบเรื่องทักทายด้วยการค้นข้อมูล | Greeting ไม่ trigger RAG |

---

## คำที่ trigger แต่ละ behavior

| User พิมพ์ | บอททำอะไร |
|---|---|
| "สวัสดี", "ขอบคุณ", "ดีครับ" | ทักทายกลับสั้นๆ ไม่ค้นข้อมูล |
| "ขอแบบละเอียด", "เชิงลึก", "วิชาการ" | เปลี่ยนโหมด Academic |
| "โอเค", "เข้าใจแล้ว", "ขอบคุณ" (หลังตอบแล้ว) | กลับโหมด Practical |
| "กลับไปเรื่องเดิม", "ต่อจากที่แล้ว" | กลับ Academic session เดิม |
| "ตอนนี้อยู่โหมดไหน" | บอกชื่อโหมดปัจจุบัน |

---

## Session Management (สำหรับทีม Ops)

### Session หมดอายุเมื่อไหร่?
- Session เก็บไว้ **7 วัน** จากนั้นลบอัตโนมัติ
- ระบบ cleanup ทุกครั้งที่มี request เข้ามา

### ดู Session ที่มีอยู่
```
GET /api/operation/sessions
→ List session IDs + วันที่สร้าง (max 20)
```

### ลบ Session
```
POST /api/operation/session/delete
Body: { "session_id": "s_xxxxxxxx" }
```

### เช็คสถานะระบบ
```
GET /api/operation/healthcheck
→ แสดง status, cache stats, rate limit stats
```

---

## ข้อมูลในระบบมาจากไหน?

```
Google Sheets (ทีมเนื้อหาดูแล)
    │
    ▼
Script ingest (ทีม tech รัน)
    │
    ▼
ฐานข้อมูล Vector Store (Chroma)
    │
    ▼
น้องสุดยอดดึงข้อมูลไปตอบ
```

**ถ้าข้อมูลผิด/ไม่ครบ:** แก้ที่ Google Sheets แล้วรัน ingest script ใหม่
ไม่ต้องแก้ที่บอทโดยตรง

---

## เมื่อบอทตอบผิดหรือมีปัญหา — ตรวจสอบเบื้องต้น

| อาการ | สาเหตุที่เป็นไปได้ | วิธีแก้เบื้องต้น |
|---|---|---|
| บอทบอก "ไม่มีข้อมูล" ทั้งที่มีข้อมูลใน Sheet | ยังไม่ได้ ingest ข้อมูลใหม่ | รัน ingest script |
| บอทถามซ้ำๆ ไม่ตอบสักที | Session อาจค้างอยู่ในสถานะแปลก | Reset session |
| ตอบช้ามาก (>30 วินาที) | OpenRouter API ช้า / timeout | ดู server logs |
| HTTP 503 | Services ไม่ได้ initialize | Restart server |
| HTTP 429 | User ส่งถี่เกินไป | รอตาม Retry-After header |
